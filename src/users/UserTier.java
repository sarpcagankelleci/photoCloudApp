package users;

public enum UserTier {
	FREE, HOBBYIST, PROFESSIONAL; // Enum type of user tiers
	// Using enum type of user tier objects to evaluate which user with specified user tier can apply the filter
}
		



